include: package:very_good_analysis/analysis_options.yaml

analyzer:
  exclude:
    - "**/*.g.dart"

linter:
  rules:
    # Match existing app style; document non-obvious APIs inline instead.
    public_member_api_docs: false
    lines_longer_than_80_chars: false
